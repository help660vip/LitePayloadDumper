## LitePayloadDumper v1.0.1

- 地址、搜索、保存目录和线程输入框支持 `Ctrl+A` 后 Delete / Backspace 快速清空
- 精简主界面标题、说明文字和布局
- 移除“旧镜像目录”和“常用启动分区”控件
- 需要旧镜像的增量分区会明确标记为不支持
- README 改为简洁的下载、使用和格式说明
- 使用新的多尺寸 Logo 作为窗口和 EXE 图标

### 下载

- Windows 7 SP1：`LitePayloadDumper_Win7.exe`
- Windows 10 / 11：`LitePayloadDumper_Win10-11.exe`

两个版本均为 64 位单文件 EXE，无需安装外部框架或运行库。可使用 `SHA256SUMS.txt` 校验文件完整性。
